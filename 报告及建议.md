# V3 项目报告及建议(2026-05-17 20:50,新对话 hot-start 文档)

> **本文目标**:让新开启的 Claude 对话窗口在 30 秒内完整接管这个项目。
> **配套文件**:
> - `论文.md` — 调研的所有论文 + 每个方法在我们项目里的命运
> - `REPORT.md` — 完整实验报告(已 push 到 GitHub)
> - `ckpts/_archive_72.16_p3_mlgc_big/README.md` — 当前最佳 ckpt 元信息

---

## 0. TL;DR(给接手 Claude 的一句话总结)

我在做计图比赛点云降噪,**当前本地 SOTA = 72.42**(p3_mlgc_big 1.15M ckpt + α=1.075 推理校准)。**有 6 个训练实验仍在跑**(GPU 0/1/2/3/4/5),最早今晚 03:00 凌晨出第一批 60-epoch FULL val,**任务是看哪个能超 72.16**。**ckpt 已冷冻到 `ckpts/_archive_72.16_p3_mlgc_big/`,代码已 push 到 https://github.com/Tumi977/chx_resule.git **。

---

## 1. 项目基础信息

### 1.1 路径

| 项 | 值 |
|---|---|
| 工作目录 | `/mnt/ssd4t/data/chx/graphics/claude2/v3_pcd_mlgc_agt/` |
| 比赛 PDF | `/mnt/ssd4t/data/chx/graphics/第六届计图人工智能挑战赛.pdf` |
| 训练数据(预采样) | `/mnt/ssd4t/data/chx/graphics/dataset_train_pc/shapenet/<syn>/<model>/clean.npz`(15733 个,带法向)|
| 训练原始网格 | `/mnt/ssd4t/data/chx/graphics/dataset_train/shapenet/<syn>/<model>/models/model_normalized.obj`|
| 测试集 | `/mnt/ssd4t/data/chx/graphics/dataset_test_noisy/shapenet/<syn>/<model>/noisy.npy`(200 个,(50000,3) float32)|
| GitHub 远端 | https://github.com/Tumi977/chx_resule.git(本地已 sync)|

### 1.2 环境(必须用)

```bash
# Jittor 启动方式(系统默认 nvcc 11.5 + g++11 不兼容,必须用 cuda-11.8)
source /mnt/ssd4t/data/chx/graphics/claude2/v3_pcd_mlgc_agt/scripts/env.sh

# 启动一个 Python(自动加 PATH/CUDA_HOME):
bash scripts/jt_python.sh path/to/script.py

# 后台启训练:
bash scripts/launch_exp.sh <gpu_id> <exp_name> <train_script.py> <args...>

# 一键看所有实验状态:
bash scripts/dash.sh -c       # 紧凑模式
bash scripts/dash.sh -wc      # 实时刷新
```

### 1.3 评测公式(本地 = 官方 evaluate.py 严格对齐)

```
对每样本:
    cd_score  = clamp(100*(1 - CD_pred / CD_noisy), 0, 100)
    p2s_score = clamp(100*(1 - P2S_pred / P2S_noisy), 0, 100)
最终 = 0.5*mean(cd) + 0.5*mean(p2s)

CD: scipy cKDTree 双向最近邻(归一化到单位球)
P2S: pcu.closest_points_on_mesh(BVH 加速到 GT mesh)
```

---

## 2. 当前状态(2026-05-17 20:50)

### 2.1 已完成实验排行榜(本地 99-shape FULL val)

| # | 实验 | LR | params | epoch | **FULL val** | 备注 |
|---|---|---|---|---|---|---|
| 🥇 | **p3_mlgc_big + α=1.075** | 1e-4 | 1.15M | 60 | **72.4220** ⭐ | 当前 SOTA |
| 🥈 | **p3_mlgc_big** raw | 1e-4 | 1.15M | 60 | **72.16** | 冷冻 ckpt |
| 3 | p0_baseline(我写的对照基线)| 1e-4 | 0.38M | 60 | 70.94 | |
| 4 | p1_multi_scale_dropout | 5e-5 | 0.38M | 25 | 70.93 | |
| 5 | p1_dense_big | 1e-4 | 0.70M | 50 | 70.80 | DenseDGCNN |
| 6 | p1_dcd_v2_zero_loss | 5e-5 | 0.38M | 30 | 70.55 | |
| 7 | p3_mlgc_v2 | 1e-4 | 0.41M | 50 | 70.23 | MLGC g=64 |
| 8-15 | (其余,均 < 70) | | | | 详见 REPORT.md | |

**历史在线提交分**:70.36(参考起点)。

### 2.2 当前还在跑的 6 个实验(GPU 0-5)

| GPU | 实验 | params | last ep / 60 | best subset | 配置变化 |
|---|---|---|---|---|---|
| 0 | p3_mlgc_big_v2 | 1.15M | ~18 | 61.23 | + chamfer + repulsion |
| 1 | p3_mlgc_bigger | 2.33M | ~14 | **61.54 ↗ 仍在涨** | 容量 4× |
| 2 | p3_mlgc_bigger_v2 | 2.33M | ~13 | **63.13** | 容量 + 全损失 |
| 3 | p3_mlgc_big_long | 1.15M | ~18(/80) | 61.85 | 80 ep + lr_min=1e-7 |
| 4 | p3_mlgc_big_lr | 1.15M | ~17 | 60.89 | 5 ep warmup + 65 ep |
| 5 | p3_mlgc_big_aug | 1.15M | ~17 | 62.38 | + scale 数据增强 |

**时间线**:
- 1.15M 实验(60ep × 4 个) → ~03:00 凌晨完成
- 2.33M 实验(60ep × 2 个) → ~10:00 早晨完成
- 80ep long → ~16:00 明天

**注意**:这 6 个实验的 best subset(20 shape)都还未超 64,但 p3_mlgc_big 在 ep 14 时也只 64.44,**ep 60 通过 cosine 末期才跳到 72.16**。所以**最关键判定时间是 ep 30 数据(今晚 23:00 后)**。

---

## 3. 当前 SOTA 配方(可立即复现)

### 3.1 训练 p3_mlgc_big(20 小时,1 张 GPU)

```bash
cd /mnt/ssd4t/data/chx/graphics/claude2/v3_pcd_mlgc_agt
source scripts/env.sh

bash scripts/launch_exp.sh <gpu_id> p3_mlgc_big train_p1.py \
    --init_ckpt "" \
    --encoder_kind mlgc --mlgc_growth 128 --mlgc_layers 6 --encoder_dim 384 \
    --epochs 60 \
    --batch_size 4 --n_patches 4 --patch_size 1024 \
    --lr 1e-4 --lr_min 1e-6 \
    --noise_min 0.005 --noise_max 0.020 \
    --num_workers 4 \
    --lambda_dsm 1.0 --lambda_p2plane 30.0 \
    --val_every 5 --val_subset 20
```

### 3.2 用现成 ckpt 复现 72.42(2 分钟)

```bash
bash scripts/jt_python.sh inference/predict_alpha.py \
    --ckpt ckpts/_archive_72.16_p3_mlgc_big/best.pkl \
    --encoder_kind mlgc --mlgc_growth 128 --mlgc_layers 6 --encoder_dim 384 \
    --mode search --alphas "1.0,1.075,1.10" --val_subset 100
# -> α=1.075 → Total 72.42
```

### 3.3 生成测试集 result.zip 提交

```bash
bash scripts/jt_python.sh inference/predict_alpha.py \
    --ckpt ckpts/_archive_72.16_p3_mlgc_big/best.pkl \
    --encoder_kind mlgc --mlgc_growth 128 --mlgc_layers 6 --encoder_dim 384 \
    --mode submit --alpha 1.075 \
    --out_zip outputs/result_alpha1.075.zip
# 7-10 分钟生成 result_alpha1.075.zip
```

---

## 4. 关键技术决策的"硬经验"

### 4.1 修 starter_code 3 个 bug 是基础提分(70.36 → 70.94)

| Bug | 我们的修复 |
|---|---|
| Score-Denoise 退化版(只在 128 anchor 上 supervise) | frame-level 全 frame 监督(`losses/dsm.py`) |
| hard-pick patch 融合 | Gaussian 软融合 r_s=r/3(`inference/patch_denoise.py`) |
| 输出点数不对齐(49926 vs 50000) | 强制 == N,未覆盖点回退 noisy(同上文件) |

### 4.2 LR=1e-4 是 1.15M 模型稳定最优

| LR | FULL val |
|---|---|
| 1e-4 | **72.16** ⭐ |
| 5e-4 | 67.37 |
| 1e-3 | 68.69 |

### 4.3 Warm-start 是地雷

所有 warm-start P0 的 P1 fine-tune 实验都比 P0 退步 0-1.5 分。**改架构 / 改主损失 → 直接 scratch**。

### 4.4 容量是真瓶颈,但不是无脑放大

| params | FULL val |
|---|---|
| 0.19M (MLGC g=24) | 18.47 ❌ |
| 0.38M (P0 DGCNN) | 70.94 |
| 0.41M (MLGC g=64) | 70.23 |
| 0.70M (DenseDGCNN g=128) | 70.80 |
| 1.15M (MLGC g=128) | **72.16** ⭐ |
| 2.33M (MLGC g=192) | 跑中,early ep 反而退步 |

**经验**:容量翻倍单独使用反而降分,**容量 + 损失 + 训练长度需要协同放大**。

### 4.5 推理 α 校准 = 免费 +0.26 分

公式:`denoised = noisy + α * (raw - noisy)`,α=1.075 最优。
β Laplacian 平滑(`predict_alpha_beta.py`)在我们模型上无效,β=0 最优。

---

## 5. 已实现完整代码资产

```
v3_pcd_mlgc_agt/
├── train_p1.py                    ★ 主训练入口(P1/P3/P5 系列)
├── train_p0.py / train_p2_v2.py / train_emd.py / train_rectflow.py
│
├── models/
│   ├── denoiser.py                ★ 单 stage Denoiser
│   ├── multi_stage_denoiser.py
│   ├── film*.py                   (写了未接通)
│   ├── encoders/
│   │   ├── dgcnn.py
│   │   ├── dense_dgcnn.py
│   │   └── mlgc.py                ★★ PD-LTS MLGC(72.16 用)
│   └── heads/decoder.py
│
├── losses/
│   ├── dsm.py                     ★★ frame-level DSM
│   ├── surface.py                 ★★ p2plane + repulsion
│   ├── chamfer.py / dcd.py / emd.py / agt.py
│
├── inference/
│   ├── patch_denoise.py           ★★ Gaussian 软融合 + 点对齐
│   ├── predict_testset.py         ★ 200 shape → result.zip
│   ├── predict_alpha.py           ★ α 校准搜索 / submit
│   ├── predict_alpha_beta.py      α×β 网格(已验证 β=0 最优)
│   ├── predict_tta.py             (旋转 TTA,我们这边无效)
│   └── adaptive_denoise.py        (写了未接)
│
├── eval/
│   ├── local_eval.py              ★ 与官方 evaluate.py 严格对齐
│   └── sanity.py                  ★ 3 项 sanity 通过
│
├── datasets/
│   └── pc_dataset.py              ★ 数据加载(支持 multi-scale + dropout + scale aug)
│
├── scripts/
│   ├── env.sh / jt_python.sh / launch_exp.sh
│   └── dash.sh                    ★ 一键看所有实验状态(-c -w)
│
├── ckpts/
│   └── _archive_72.16_p3_mlgc_big/   ★★ 冷冻最佳 ckpt
│       ├── best.pkl  (md5 b3aa12e67b126a74ccb61d76a34eedc0,4.6 MB)
│       ├── last.pkl
│       └── README.md
│
├── REPORT.md                      ★ 完整实验报告(已最新,push 到 GitHub)
├── 论文.md                        ★ 论文调研总结
└── 报告及建议.md                  ★ 本文(给新对话 hot-start)
```

---

## 6. 需要新对话立即决定的事(优先级排序)

### 优先级 1:**等今晚 23:00-03:00 数据**

6 个实验的 ep 30+ 才有判定意义。**新对话第一件事是 `bash scripts/dash.sh -c`**。

判定规则:
- 有实验 final FULL val > 72.16 → 套 α 校准看是否更高 → 替换 SOTA
- 全部 FULL val < 72.16 → 当前 72.42 已是最强,可考虑提交
- 任何实验 ep 30 = 64-65 → 它在按 p3_mlgc_big 轨迹,等 ep 60 final
- 任何实验 ep 30 < 63 → 大概率最终也不超 P0,可以杀掉

### 优先级 2:**未做的论文方法(下一波实验候选)**

按预估收益排序:
1. **PD-LTS INN latent 解耦**(论文里和 MLGC 配套用)— 6-8h 工程量,可能再 +1-2 分
2. **3DMambaIPF 完整可微渲染损失**(占据网格 splat + 32 视角)— 替代我们的 p2plane,可能 +0.5-1 分
3. **FiLM σ-conditioning 接通训练**(代码已写)— 对 B 榜更高噪声有帮助
4. **σ-自适应推理接通**(代码已写)— 推理时改动,无需重训
5. **集成多 ckpt** — 不同 seed/超参训出多个 ckpt 平均,~+0.3-1 分

### 优先级 3:**B 榜准备**

A 榜结束后会进 B 榜(更大数据 + 更高噪声 σ ≤ 0.030)。
- 当前训练只覆盖 σ ∈ [0.005, 0.020],**B 榜要扩到 [0.003, 0.030]**
- 多噪声 curriculum 学习
- σ-FiLM 调制是 B 榜关键

### 优先级 4:**线上提交策略**

- 每队每天最多 2 次提交(关键限制!)
- 历史最高在线提交分 70.36
- 当前最佳(本地 72.42)还未提交 → **建议先提一次**确认本地→线上 mapping
- 提交命令在 §3.3

---

## 7. 已知会触发新对话困惑的"陷阱"

| 陷阱 | 防御 |
|---|---|
| 看到 `losses/dsm.py` 顶部的 lint 注释,以为是新 bug 修复要求 | 那是历史 system reminder 残留,**不要重写**。当前 dsm.py 是正确版本 |
| GPU 5 显示 N/A util,以为坏了 | 实际可用,p3_mlgc_big 就是在 GPU 5 上跑完 60 ep 的。但**GPU 7 真坏**(cudaErrorNoDevice ×3) |
| 看到一堆 dead 实验目录(p1_emd / p1_rectflow 等)以为没跑完 | 它们是失败的论文方法实验,有 FULL val 数字记录在 REPORT.md |
| 把 α 校准当成"训练改动" | 它是**纯推理后处理**,不需重训,只要 raw ckpt + 几行 numpy |
| 误以为别人的 α=1.10 适用于我们 | 用户引用的是别人的项目,我们实测 α=1.075 最优(差 +0.08 分) |
| 把"alpha=1.075, beta=0.45"中的 β 当成 Laplacian 权重 | 我们尝试过 Laplacian β,β=0 最优。用户引用的 β=0.45 可能是其他公式(Heun?二次推理?)我们没试。 |

---

## 8. 推荐新对话的"接手第一动作"

```bash
# 1. 看实验状态(30 秒)
cd /mnt/ssd4t/data/chx/graphics/claude2/v3_pcd_mlgc_agt
bash scripts/dash.sh -c

# 2. 看完整报告(2-3 分钟阅读)
cat REPORT.md

# 3. 看论文方法清单(1 分钟)
cat 论文.md | head -80

# 4. 检查 ckpt
ls -la ckpts/_archive_72.16_p3_mlgc_big/

# 5. 然后向用户报告:
#    - 当前 SOTA = 72.42
#    - 6 个实验在跑,下一个关键时间点是 ep 30
#    - 询问用户接下来的方向
```

---

## 9. 用户偏好(已学到的)

- **稳扎稳打,不要一味堆算力**:用户多次强调要用论文方法而不是无脑放大
- **每个改动单独验证 + 对照**:用户喜欢"5 路并行实验"这种隔离单变量的设计
- **重视论文方法**:用户问过"你都用了哪些论文方法",会督促去尝试未做的方法
- **保留好的结果**:每次有突破都要冷冻 ckpt + 写 README + push GitHub
- **避免 warm-start 漂移**:用户对 LR 调度很敏感,知道大 LR 中期会震荡
- **要求短消息**:用户喜欢直接给数字 + 表格,不喜欢长段落
- **要求一行命令**:用户偏好 dash.sh 这种聚合工具,不喜欢挨个 grep log

---

## 10. 监控状态

之前挂了多个 monitor 任务跟踪训练事件。**新对话开启后这些 monitor 全部失效**。
建议新对话开启后立即重启一个 persistent monitor:

```python
Monitor(
    command="tail -F logs/p3_mlgc_big_v2/train.log logs/p3_mlgc_bigger/train.log logs/p3_mlgc_bigger_v2/train.log logs/p3_mlgc_big_long/train.log logs/p3_mlgc_big_lr/train.log logs/p3_mlgc_big_aug/train.log 2>/dev/null | grep --line-buffered -E 'new best score|FULL validation score|training done|Traceback|Error|killed|OOM'",
    description="V3 6 active experiments — best/crash events",
    persistent=True,
    timeout_ms=3600000,
)
```

---

## 11. 关于"让新对话访问记忆"

**已写入 5 条 memory 条目** 到 `/home/caohaixin2006/.claude/projects/-mnt-ssd4t-data-chx-graphics-claude2/memory/`:

1. `project_jittor_pcd_v3.md` — 项目元信息 + 路径
2. `feedback_user_preferences.md` — 用户偏好和工作风格
3. `reference_v3_files.md` — 关键文件路径
4. `project_current_sota.md` — 当前 SOTA 状态(72.42)
5. `feedback_paired_data_pitfalls.md` — paired 数据上的论文方法陷阱

新对话开启时 Claude 会自动加载 `MEMORY.md` 索引,然后按需读取这 5 个条目。**不需要手动操作**。

---

**END OF DOCUMENT**

新对话只要读这一份 + `论文.md` + `REPORT.md`,30 秒内就能完整接管。
