# 论文调研总结(V3 项目使用)

**项目位置**:`/mnt/ssd4t/data/chx/graphics/claude2/v3_pcd_mlgc_agt/`
**赛题**:第六届计图(Jittor)AI 挑战赛 · 赛道二 · 基于深度学习的点云降噪
**评测**:本地 99-shape FULL validation,σ=0.01,CD + P2S 各占 50%(百分制以含噪输入为零分)

---

## 0. 速查:每个论文方法在我们项目里的"命运"

| 状态 | 论文方法 | 说明 |
|---|---|---|
| ✅ **生效** | Score-Denoise frame-DSM | 主损失(`losses/dsm.py`) |
| ✅ **生效** | 法向切平面 P2S(3DMambaIPF 简化) | `losses/surface.py p2plane_loss` |
| ✅ **生效** | Gaussian patch stitching r/3 | 推理(`inference/patch_denoise.py`) |
| ✅ **生效** | DGCNN dynamic EdgeConv | 所有 encoder 基础 |
| ✅ **生效** | PD-LTS MLGC dense skip + in-projection | `models/encoders/mlgc.py`(72.16 ckpt 用的就是它)|
| ✅ **生效** | 推理时 α 线性校准 | `inference/predict_alpha.py`(+0.26 → 72.42) |
| ⚠️ **失败** | IterativePFN AGT NN-projection | paired 数据上 NN 退化为恒等,loss=0 |
| ⚠️ **失败** | DCD (NeurIPS'21) | warm-start 后 d² 极小,DCD 退化为普通 CD |
| ⚠️ **失败** | StraightPCF rect-flow velocity | warm-start 漂走 + paired 数据下范式优势消失 |
| ⚠️ **失败** | StraightPCF K=2 multi-iter TTA | σ ∈ [0.5%, 2%] 上过迭代,CD 退步 -3 |
| ⚠️ **失败** | Sinkhorn EMD 主损失(PD-LTS 风格) | warm-start 漂走 + 与 frame-DSM 方向冲突 |
| ⚠️ **失败** | K-step shared iterative(IterativePFN) | 同 AGT,paired 数据失效 |
| ⚠️ **失败** | Laplacian 后处理 β | 我们模型 β=0 最优,P2S 微涨但 CD 大跌净亏 |
| ⚠️ **失败** | MLGC light(growth=24)| 容量太小(0.19M)严重 underfit |
| ❌ **未做** | PD-LTS INN latent 解耦 | 工程量 6-8h,PD-LTS 灵魂方法 |
| ❌ **未做** | StraightPCF DistanceModule | 自适应步长 |
| ❌ **未做** | StraightPCF K-coupled velocity | |
| ❌ **未做** | ASDN Recoverability + 自适应停步 | AAAI'25 |
| ❌ **未做** | 3DMambaIPF 完整 occupancy 渲染 | p2plane 是简化版 |
| ❌ **未做** | FiLM σ-conditioning 接通训练 | 代码 `models/film.py` 已写未接 |
| ❌ **未做** | σ-自适应推理接通 | 代码 `inference/adaptive_denoise.py` 已写未接 |

---

## 1. Score-Denoise (Luo & Hu, ICCV 2021)

### 核心思路
基于 score matching 训练去噪器:网络预测每个点指向干净表面的"梯度"(displacement)。

### 关键技术:**Frame-level DSM 损失**
- starter_code 把它简化成"只在 N 个随机 anchor 上算 loss"——丢失大量梯度信号
- 论文原版:每个 anchor 取 `frame_k` 个邻居,**对所有 frame 点的位移做监督**(密度 ×32)
- target = `mean(clean_k=4 nearest pc_clean nbrs of frame_pts_noisy[i]) - frame_pts_noisy[i]`

### 在我们项目里的实现
`losses/dsm.py`:`frame_dsm_loss(pred_disp, pc_noisy, pc_clean, n_anchor=128, frame_k=32, clean_k=4, dsm_sigma=0.01)`

### 贡献
**+1.0~2.5 分**(P0 baseline 70.94 的核心损失)

---

## 2. IterativePFN (de Silva Edirimuni et al., CVPR 2023)

### 核心思路
- 网络内部展开 T=4 次"迭代降噪",每步学一个噪声等级
- **Adaptive Ground Truth (AGT) loss**:第 τ 步 GT 不是干净点,而是噪声水平 σ_τ 重新加噪过的点云

### 关键技术
1. **AGT 公式**:`Y_τ = clean + σ_τ · ξ_τ`,σ_τ 几何衰减(δ=2)
2. **NN-projection target**:`target_τ_i = NN(x_i^{τ-1}, Y_τ) - x_i^{τ-1}`
3. **Gaussian-weighted patch stitching**:推理时 r_s = r/3 软融合

### 在我们项目里的命运
- ✅ Patch stitching 直接采用(推理路径)
- ⚠️ AGT NN-projection **在 paired 数据上失败**:NN 退化为恒等,loss=0
  - 修复尝试 1(`p1_iter_K3_v2`):scratch + K=3 共享权重 → loss 仍 ≈ 0
  - 修复尝试 2(`p2_agt_T4_v3`):改用"显式生成 σ_τ 噪声 + paired GT 直接监督" → 训练慢且不收敛
  - 根本原因:论文设定是 unpaired (PointCleanNet 风格);我们 dataset 是严格 paired,NN 投影失效

---

## 3. PD-LTS (Mao et al., CVPR 2024) ★ 最有用的论文

### 核心思路
两个模块叠加:
1. **MLGC encoder**:Multi-Level Graph Convolution,dense skip-concat
2. **INN(可逆神经网络)**:在 latent space 解耦"几何信号 vs 噪声信号",再可逆映射回干净点

### 关键技术:**MLGC encoder**
- In-projection:3D 坐标过 MLP 升到 12 维(PointNet 风格)
- 每层 EdgeConv 输入 = `concat(原坐标, in_proj, 之前所有层输出)`(DenseNet 风格)
- 6 层,每层 growth=128
- Final fusion:concat(all layers) → Conv1d → embedding 384

### 在我们项目里的实现
- ✅ MLGC encoder:`models/encoders/mlgc.py`
- ✅ 用配置 `growth=128, n_layers=6, embedding_dim=384` 得到 1.15M 参数
- ❌ **INN 没实现**(PD-LTS 真正灵魂,工程量 6-8h)

### 贡献
**MLGC encoder 单独 = +1.22 分**(P0 70.94 → p3_mlgc_big 72.16)

### 额外发现
- MLGC growth=24(light 版)= 0.19M 参数 **完全无法训练**(p3_mlgc 跑出 18.47)
- **必须容量足够大**(我们用 1.15M,3× P0)

---

## 4. StraightPCF (de Silva Edirimuni et al., CVPR 2024)

### 核心思路
- 把噪声点沿"直线轨迹"推到表面
- VelocityModule 预测**恒定**速度场 v_θ(x_t),DistanceModule 预测每点"还要走多远"

### 关键技术
1. **Rectified-flow velocity field**:训练时 t ~ U(0,1),`x_t = (1-t)·noisy + t·clean`,target velocity = `clean - noisy`(直线常速度)
2. **K-coupled velocity (K=2)**:推理时 2 步 Euler 积分
3. **niters 自适应**:σ=1% 跑 1 轮,σ=2% 跑 2 轮,σ=3% 跑 3 轮

### 在我们项目里的命运
- ⚠️ Rect-flow velocity(`train_rectflow.py`):**FULL val=66.86 失败**
  - warm-start P0 后 LR=5e-5 把 P0 ckpt 漂走
  - paired 数据下 velocity = displacement,优势消失
- ⚠️ K=2 TTA(`inference/predict_tta.py`):**67.6 → 65.3,反退 2.3 分**
  - σ ∈ [0.5%, 2%] 上是过迭代
- ❌ DistanceModule、K-coupled velocity 都未实现

---

## 5. 3DMambaIPF (Zeng et al., AAAI 2025)

### 核心思路
- Mamba(状态空间模型)做 encoder
- **可微渲染损失**:用 occupancy splat + 三平面 32 视角投影,L1 loss

### 关键技术
- 可微渲染让模型直接对齐 P2S 评测指标的几何含义

### 在我们项目里的命运
- ❌ Mamba 没实现(Jittor 无 native Mamba kernel)
- ✅ **简化版**:`losses/surface.py p2plane_loss`(法向投影距离² 替代渲染)
  - 几何意义:沿 GT 法向的位移误差 = 点到表面距离的近似
  - 我们数据集 `clean.npz` 自带 GT 法向,几乎零成本

### 贡献
**+1.5~2.5 分**(P2S 项从 ~80 提升到 84-86)

---

## 6. ASDN (AAAI 2025)

### 核心思路
- 每个点动态决定"何时停止降噪"
- **Recoverability factor**:估计每个点的剩余噪声水平,σ_pred 高的继续降,低的停止

### 在我们项目里的命运
- ❌ 未实现(工程量大,优先级低)
- 备用代码 `inference/adaptive_denoise.py` 写了一半但未接管线

---

## 7. DCD: Density-aware Chamfer Distance (Wu et al., NeurIPS 2021)

### 核心思路
- 普通 Chamfer 不惩罚多对一塌缩(N 个 pred 都映到同一 GT 点 → 0 损失)
- DCD 通过 `weight_i = 1 - exp(-α·n_j/N)` 软-惩罚拥挤分配
- `(1 - exp(-d²))` 把 loss bound 到 [0,1]

### 在我们项目里的命运
- ✅ 实现:`losses/dcd.py`
- ⚠️ **应用时机错了**:warm-start 后 d² 已极小,DCD 退化为普通 CD
- 修复尝试 `p1_dcd_v3`(scratch + α=50):FULL val 69.11(略低于 P0)
- 结论:**这是个早期监督**,模型已收敛后没价值

---

## 8. Sinkhorn EMD(基础,Cuturi NIPS 2013)

### 核心思路
- 真正的 Earth Mover Distance 用 Sinkhorn 迭代近似
- PD-LTS 论文用纯 EMD 训练就达到 SOTA

### 在我们项目里的命运
- ✅ 实现:`losses/emd.py`(50 步 Sinkhorn,纯 Jittor)
- ⚠️ `train_emd.py p1_emd`:**FULL val 失败**
  - warm-start P0 + EMD 改主损失方向 → 漂离 P0 最优点
  - 训练初期 score 跳到 60.43(ep 2),然后退步到 57 卡死

---

## 9. DGCNN (Wang et al., TOG 2019)

### 核心技术
- **Dynamic Edge Convolution**:每层根据当前特征空间重建 KNN 图
- 4 层 EdgeConv 堆叠

### 在我们项目里
- ✅ 标准 DGCNN encoder:`models/encoders/dgcnn.py`(P0 baseline 用)
- ✅ 工具函数 `knn_idx`、`gather_neighbors`(MLGC 也复用)

---

## 10. 综述 arXiv 2508.17011 (Aug 2025)

### 关键摘录(从全文 PDF 提取)
- PU-Net 50K @ σ=1% 排行:**PD-LTS** CD=0.472,P2M=0.296(SOTA)
- ASDN 紧随其后:CD=0.499,P2M=0.308
- 3DMambaIPF:CD=0.589,P2M=0.291
- StraightPCF 在 σ=3% 是 SOTA
- **80 分对应文献里的 ~PD-LTS 水平**(我们当前 72.42 ≈ IterativePFN/Score-Denoise 之间)

---

## 11. 失败实验的"教训库"

### 11.1 损失权重平衡

| 错误 | 后果 |
|---|---|
| `λ_repulsion = 0.05` | rep 数值大,主导训练 → 网络优化"打散点"而非"贴表面",**13 分跌** |
| `λ_repulsion = 0.15` | 完全崩溃,score = 0 |
| `λ_dcd = 1.0` + warm-start | DCD loss 永远 0,无梯度 |

**经验**:新损失加入前,先看它的数值量级。若 `λ_new × value_new > λ_main × value_main`,会立刻主导训练。

### 11.2 LR 选择(惨痛对比)

```
LR=1e-4 (p3_mlgc_big):    72.16  ⭐
LR=5e-4 (p3_mlgc_best_v3): 67.37
LR=1e-3 (p3_mlgc_best):    68.69
```

**经验**:大 LR 早期收敛快(ep 5 优势),但中期震荡(ep 10-30 退步),即使 cosine 末期降到 1e-5 也无法恢复早期破坏的权重。**1e-4 是 1.15M 模型的稳定最优**。

### 11.3 Warm-start 的双面性

- 用 P0 ckpt warm-start P1 fine-tune:**所有 P1 实验都比 P0 退步 0-1.5 分**
- 原因:P0 已是当前损失的最优点,任何方向 fine-tune 都漂离

**经验**:架构改了或损失主体改了,**直接 scratch**;微调用 1e-5 或更小 LR。

### 11.4 论文方法在 paired 数据上的退化(高发坑)

| 方法 | 论文设定 | 我们设定 | 结果 |
|---|---|---|---|
| AGT NN-projection | unpaired | paired | NN 退化为恒等 |
| DCD 损失 | scratch | warm-start | d² 极小,退化为普通 CD |
| K=2 multi-step | σ ≥ 2% | σ ∈ [0.5%, 2%] | 过迭代,CD -3 |
| Laplacian β>0 | 弱模型 | 强模型 | 抹平细节 |

**经验**:**每个论文方法都要先看实验设定与我们的差异**。能照搬的少。

---

## 12. 引用的论文链接

1. Luo & Hu, **Score-Based Point Cloud Denoising**, ICCV 2021. arXiv:2107.10981
2. de Silva Edirimuni et al., **IterativePFN**, CVPR 2023. arXiv:2304.01529
3. de Silva Edirimuni et al., **StraightPCF**, CVPR 2024. arXiv:2405.08322
4. Mao et al., **PD-LTS**, CVPR 2024.
5. Zeng et al., **3DMambaIPF**, AAAI 2025. arXiv:2404.05522
6. Wu et al., **DCD**, NeurIPS 2021.
7. Wang et al., **DGCNN**, TOG 2019.
8. Cuturi, **Sinkhorn Distances**, NIPS 2013.
9. **A Survey of Deep Learning-based Point Cloud Denoising**, arXiv:2508.17011 (2025)
